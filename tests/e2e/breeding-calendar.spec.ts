import { expect, test, type Page } from "@playwright/test";
import { formatLitterJournalBusinessDate } from "../../src/features/litter-journal/date";
import { E2E_OWNER_EMAIL, E2E_OWNER_PASSWORD, runE2eSqlSync } from "./helpers/supabase";

test.setTimeout(240_000);
const org="20000000-0000-4000-8000-000000000001",owner="10000000-0000-4000-8000-000000000001",p="9f260050-0000-4000-8000-0000000000",f="9f260051-0000-4000-8000-0000000000",q=(v:string)=>`'${v}'`,sql=(v:string)=>runE2eSqlSync(v),day=formatLitterJournalBusinessDate(new Date());
const id={mother:`${p}01`,a:`${p}02`,b:`${p}03`,aTask:`${p}04`,bTask:`${p}05`,window:`${p}06`,done:`${p}07`,foreignOrg:`${f}01`,foreignMother:`${f}02`,foreignLitter:`${f}03`,foreignTask:`${f}04`};
function clean(){sql(`delete from public.litter_care_tasks where id::text like '9f26005%';delete from public.litters where id::text like '9f26005%';delete from public.animals where id::text like '9f26005%';delete from public.organizations where id::text like '9f260051-%';`)}
function counts(){return JSON.parse(sql(`select json_build_object('tasks',(select count(*) from public.litter_care_tasks where id::text like '9f26005%'),'litters',(select count(*) from public.litters where id::text like '9f26005%'),'animals',(select count(*) from public.animals where id::text like '9f26005%'),'organizations',(select count(*) from public.organizations where id::text like '9f260051-%'))::text`))}
function fixture(){sql(`insert into public.animals(id,organization_id,call_name,species,breed,sex,status,ownership_status,created_by,updated_by) values(${q(id.mother)}::uuid,${q(org)}::uuid,'E2E mère','dog','Golden Retriever','female','breeding','owned',${q(owner)}::uuid,${q(owner)}::uuid);insert into public.litters(id,organization_id,name,species,breed,mother_id,status,created_by,updated_by) values(${q(id.a)}::uuid,${q(org)}::uuid,'E2E Alpha','dog','Golden Retriever',${q(id.mother)}::uuid,'birth_expected',${q(owner)}::uuid,${q(owner)}::uuid),(${q(id.b)}::uuid,${q(org)}::uuid,'E2E Bravo','dog','Golden Retriever',${q(id.mother)}::uuid,'birth_expected',${q(owner)}::uuid,${q(owner)}::uuid);insert into public.litter_care_tasks(id,organization_id,litter_id,source,occurrence_no,item_kind,category,target_scope,title,planned_for,retained_starts_on,retained_ends_on,priority,schedule_source,is_schedule_locked,status,creation_command_id,created_by,updated_by) values(${q(id.aTask)}::uuid,${q(org)}::uuid,${q(id.a)}::uuid,'manual',1,'task','veterinary','litter','E2E Alpha visite',${q(day)}::date,null,null,'normal','suggested',false,'planned',${q(`${p}20`)}::uuid,${q(owner)}::uuid,${q(owner)}::uuid),(${q(id.bTask)}::uuid,${q(org)}::uuid,${q(id.b)}::uuid,'manual',1,'task','vaccination','litter','E2E Bravo vaccin',${q(day)}::date,null,null,'normal','suggested',false,'planned',${q(`${p}21`)}::uuid,${q(owner)}::uuid,${q(owner)}::uuid),(${q(id.window)}::uuid,${q(org)}::uuid,${q(id.a)}::uuid,'manual',1,'window','veterinary','litter','E2E fenêtre',null,${q(day)}::date,${q(day)}::date,'normal','suggested',false,'planned',${q(`${p}22`)}::uuid,${q(owner)}::uuid,${q(owner)}::uuid),(${q(id.done)}::uuid,${q(org)}::uuid,${q(id.b)}::uuid,'manual',1,'task','veterinary','litter','E2E traitée',${q(day)}::date,null,null,'normal','suggested',false,'planned',${q(`${p}23`)}::uuid,${q(owner)}::uuid,${q(owner)}::uuid);update public.litter_care_tasks set status='done',resolution_command_id=${q(`${p}24`)}::uuid,resolved_at=now(),resolved_timezone_name='Europe/Paris',resolved_by=${q(owner)}::uuid where id=${q(id.done)}::uuid;insert into public.organizations(id,name,slug) values(${q(id.foreignOrg)}::uuid,'E2E étrangère','e2e-foreign');insert into public.animals(id,organization_id,call_name,species,breed,sex,status,ownership_status,created_by,updated_by) values(${q(id.foreignMother)}::uuid,${q(id.foreignOrg)}::uuid,'E2E étrangère mère','dog','Golden Retriever','female','breeding','owned',${q(owner)}::uuid,${q(owner)}::uuid);insert into public.litters(id,organization_id,name,species,breed,mother_id,status,created_by,updated_by) values(${q(id.foreignLitter)}::uuid,${q(id.foreignOrg)}::uuid,'E2E étrangère','dog','Golden Retriever',${q(id.foreignMother)}::uuid,'birth_expected',${q(owner)}::uuid,${q(owner)}::uuid);insert into public.litter_care_tasks(id,organization_id,litter_id,source,occurrence_no,item_kind,category,target_scope,title,planned_for,priority,schedule_source,is_schedule_locked,status,creation_command_id,created_by,updated_by) values(${q(id.foreignTask)}::uuid,${q(id.foreignOrg)}::uuid,${q(id.foreignLitter)}::uuid,'manual',1,'task','veterinary','litter','E2E étrangère secrète',${q(day)}::date,'normal','suggested',false,'planned',${q(`${f}20`)}::uuid,${q(owner)}::uuid,${q(owner)}::uuid);`)}
async function login(page:Page){await page.goto('/login');await page.getByLabel('Email').fill(E2E_OWNER_EMAIL);await page.getByLabel('Mot de passe').fill(E2E_OWNER_PASSWORD);await page.getByRole('button',{name:'Se connecter'}).click();await expect(page).not.toHaveURL(/\/login$/)}
test('calendrier global isolé',async({page})=>{clean();expect(counts()).toEqual({tasks:0,litters:0,animals:0,organizations:0});try{fixture();const before=sql('select count(*)::text from public.litter_care_tasks');await login(page);await page.goto('/calendar');for(const title of ['E2E Alpha visite','E2E Bravo vaccin','E2E fenêtre'])await expect(page.getByText(title)).toBeVisible();for(const title of ['E2E traitée','E2E étrangère secrète'])await expect(page.getByText(title)).toHaveCount(0);await page.getByRole('link',{name:'Semaine'}).click();await page.getByRole('link',{name:'Agenda'}).click();const response=await page.request.get('/calendar/export');const ics=await response.text();expect(response.headers()['content-type']).toBe('text/calendar; charset=utf-8');expect(response.headers()['cache-control']).toBe('private, no-store');for(const title of ['E2E Alpha visite','E2E Bravo vaccin','E2E fenêtre'])expect(ics).toContain(title);for(const title of ['E2E traitée','E2E étrangère secrète',id.a,id.foreignLitter])expect(ics).not.toContain(title);expect(sql('select count(*)::text from public.litter_care_tasks')).toBe(before);const anon=await page.context().browser()!.newContext();expect((await anon.request.get('http://127.0.0.1:3100/calendar/export')).url()).toContain('/login');await anon.close()}finally{clean();expect(counts()).toEqual({tasks:0,litters:0,animals:0,organizations:0})}});
