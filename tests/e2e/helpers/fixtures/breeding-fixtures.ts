import { randomUUID } from "node:crypto";
import { type FixtureTable, type SqlExecutor, createE2eFixtureRegistry } from "./fixture-registry";

type Registry = ReturnType<typeof createE2eFixtureRegistry>;
type Base = { id?: string; organizationId: string; ownerId: string; namespace?: string };
const q = (value: string) => `'${value.replaceAll("'", "''")}'`;
const id = (value?: string) => value ?? randomUUID();
const insert = async (execute: SqlExecutor, table: FixtureTable, values: Record<string, string | null>, registry: Registry) => {
  const entityId = values.id!; await execute(`insert into public.${table} (${Object.keys(values).join(",")}) values (${Object.values(values).map((value) => value === null ? "null" : q(value)).join(",")})`); registry.register(table, entityId); return entityId;
};
export async function createTestOrganization(execute: SqlExecutor, registry: Registry, overrides: Partial<{id:string;name:string;slug:string}> = {}) { const entityId=id(overrides.id); const ns=registry.namespace.replace(/[^a-z0-9]/gi,"-").toLowerCase().slice(-24); return insert(execute,"organizations",{id:entityId,name:overrides.name ?? `E2E ${registry.namespace}`,slug:overrides.slug ?? `e2e-${ns}-${entityId.slice(0,8)}`},registry); }
export async function createTestAnimal(execute: SqlExecutor, registry: Registry, input: Base & Partial<{callName:string;sex:"female"|"male"}>) { const entityId=id(input.id); return insert(execute,"animals",{id:entityId,organization_id:input.organizationId,call_name:input.callName ?? `E2E animal ${registry.namespace}`,species:"dog",breed:"Golden Retriever",sex:input.sex ?? "female",status:"breeding",ownership_status:"owned",created_by:input.ownerId,updated_by:input.ownerId},registry); }
export async function createTestLitter(execute: SqlExecutor, registry: Registry, input: Base & { motherId:string } & Partial<{name:string}>) { const entityId=id(input.id); return insert(execute,"litters",{id:entityId,organization_id:input.organizationId,name:input.name ?? `E2E litter ${registry.namespace}`,species:"dog",breed:"Golden Retriever",mother_id:input.motherId,status:"birth_expected",created_by:input.ownerId,updated_by:input.ownerId},registry); }
type TaskInput = Base & {litterId:string; title?:string; day:string};
async function task(execute:SqlExecutor, registry:Registry, input:TaskInput, extra:Record<string,string|null>) { const entityId=id(input.id); return insert(execute,"litter_care_tasks",{id:entityId,organization_id:input.organizationId,litter_id:input.litterId,source:"manual",occurrence_no:"1",item_kind:"task",category:"veterinary",target_scope:"litter",title:input.title ?? `E2E task ${registry.namespace}`,planned_for:input.day,priority:"normal",schedule_source:"suggested",is_schedule_locked:"false",status:"planned",creation_command_id:randomUUID(),created_by:input.ownerId,updated_by:input.ownerId,...extra},registry); }
export const createPlannedLitterCareTask = (execute:SqlExecutor, registry:Registry, input:TaskInput) => task(execute,registry,input,{});
export async function createPlannedLitterCareWindow(execute:SqlExecutor, registry:Registry, input:TaskInput & {startsOn:string; endsOn:string}) { if (input.endsOn < input.startsOn) throw new Error("E2E window end must not precede start"); const entityId=id(input.id); return insert(execute,"litter_care_tasks",{id:entityId,organization_id:input.organizationId,litter_id:input.litterId,source:"manual",occurrence_no:"1",item_kind:"window",category:"veterinary",target_scope:"litter",title:input.title ?? `E2E window ${registry.namespace}`,planned_for:null,retained_starts_on:input.startsOn,retained_ends_on:input.endsOn,priority:"normal",schedule_source:"suggested",is_schedule_locked:"false",status:"planned",creation_command_id:randomUUID(),created_by:input.ownerId,updated_by:input.ownerId},registry); }
export const createResolvedLitterCareTask = (execute:SqlExecutor, registry:Registry, input:TaskInput) => task(execute,registry,input,{status:"done",resolution_command_id:randomUUID(),resolved_at:new Date().toISOString(),resolved_timezone_name:"Europe/Paris",resolved_by:input.ownerId});
